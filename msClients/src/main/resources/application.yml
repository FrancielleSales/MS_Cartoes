spring:
  application:
    name: msClients

server:
  port: 0

eureka:
  client:
    service-url:
      defaultCone: http://localhost:8761/eureka
    instance:
      instance-id: $(spring.application.name:$(spring.application.instance_id:${random.value})