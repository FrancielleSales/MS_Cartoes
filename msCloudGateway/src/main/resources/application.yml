spring:
  application:
    name: msCloudGateway

  cloud:
    gateway:
      discovery:
        locator:
          enable: true

server:
  port: 8080

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
